package at.ac.fhcampuswien.trackingdevice.helpers;


public interface Localizer {
    // TODO: implement findPortal method and c&p the following code to override method - but DON'T CHANGE it!
    /*
    void findPortal(String coordinates);

    @Override
    public void findPortal(String argument) {
        RiddleSceneBaseController riddleSceneBaseController = new RiddleSceneBaseController("5");
        Riddle5SceneController.portalTrackerActive = true;
        riddleSceneBaseController.submit(argument, (response) -> {Riddle5SceneController.terminalMessage = response.get("msg").toString();});
    }
     */
}
